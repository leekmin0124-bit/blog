import { defineConfig } from 'vite';

export default defineConfig({
  // Vite configuration options
  // For a simple static site, default options are often sufficient.
  // You might add options here later for things like proxying API requests
  // if a backend server is introduced.
});
